# Run what-if for a specific app/env
parameters:
  app: ""
  env: ""
  subscriptionId: ""
  resourceGroupName: ""
  location: ""
  envParamFile: ""
  inventoryFile: ""

steps:
- bash: |
    set -euo pipefail
    echo "TODO: run subscription-scope what-if to ensure RG exists"
    echo "TODO: run RG-scope what-if via deployment stack update"
    echo "TODO: parse what-if to detect deletes and output a flag"
  displayName: What-if (TODO)
